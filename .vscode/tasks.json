{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-directory",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "type": "shell",
            "command": "mkdir",
            "args": [
                "-p",
                "${workspaceFolder}/build"
            ],
            "group": "build",
            "dependsOrder": "sequence"
        },
        {
            "dependsOn": [
                "build-directory"
            ],
            "dependsOrder": "sequence",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "label": "cxpm-build",
            "type": "shell",
            "command": "cxpm",
            "args": [
                "-b",
                "${workspaceFolder}"
            ],
            "group": "build",
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": "$gcc"
        }
    ]
}